<div id="{{ tabs_id }}" class="tabs">
    <ul>
        {% for tab in tabs %}
            <li data-content-id="{{ tab.tab_id }}" class="tab_title">{{ tab.title }}</li>
        {% endfor %}
    </ul>
    {{ tab_contents }}
</div>
